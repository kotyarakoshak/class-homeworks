#include <iostream>
#include <fstream>
#include <sstream>
#include <vector>
#include <stdexcept>
using namespace std;

int n, m;

bool correct (int x, int y) {return x >= 0 && x < n && y >= 0 && y < m;}

int main()
{
    int a[100][100];
    //int dir[2][2] = {{0,1};{0,-1}};
    int x, y = 0;
    int cnt = 1;
    int n, m;
    //cin >> n >> m;
    //while (cnt < n*m)
    //{
    //    a[x][y] = cnt;
    //    cnt++;
    //    int nx = x + dir[currdir][0]
    //}
}
